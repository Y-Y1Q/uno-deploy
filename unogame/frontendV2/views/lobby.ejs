<!doctype html>
<html lang="en">
  <!-- values render from BE route-->
  <input type="hidden" name="game-id" id="game-id" value="<%= gameId %>" />

  <input
    type="hidden"
    name="current-user-id"
    id="current-user-id"
    value="<%= user.id %>"
  />
  <input
    type="hidden"
    name="current-user-name"
    id="current-user-name"
    value="<%= user.username %>"
  />

  <% if(errorMsg && errorMsg.length> 0) { %>
  <script>
    alert("<%= errorMsg %>");
  </script>
  <% } %>

  <head>
    <%- include("./layout/header") %>
    <title>Lobby</title>
  </head>

  <%- include("./layout/nav-bar") %>

  <body
    class="bg-gray-200 flex flex-col items-center justify-center min-h-screen"
  >
    <div class="container mx-auto px-4">
      <div class="flex justify-between">
        <!-- Toggleable Container with Fixed Height and Tailwind CSS -->
        <div class="w-1/3 border border-red-500 p-4">
          <button
            id="toggleContent"
            class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-1 px-4 rounded mb-4"
          >
            Games
          </button>

          <div class="overflow-y-auto">
            <div id="gamesContainer" class="hidden h-[700px] overflow-y-auto">
              <h2 class="text-lg font-bold mb-4 text-red-500">
                Games Container
              </h2>
              <div class="border-l-4 border-red-500 pl-4 mb-4">
                <h4 class="font-bold block mb-2">Current Games</h4>
                <ul>
                  <li>
                    Game 1
                    <button
                      class="flex justify-end mb-2 bg-red-500 text-white px-2 py-1 rounded"
                    >
                      Exit
                    </button>
                  </li>
                  <li>
                    Game 2
                    <button
                      class="flex justify-end mb-2 bg-red-500 text-white px-2 py-1 rounded"
                    >
                      Exit
                    </button>
                  </li>
                  <li>
                    Game 3
                    <button
                      class="flex justify-end mb-2 bg-red-500 text-white px-2 py-1 rounded"
                    >
                      Exit
                    </button>
                  </li>
                  <li>
                    Game 4
                    <button
                      class="flex justify-end mb-2 bg-red-500 text-white px-2 py-1 rounded"
                    >
                      Exit
                    </button>
                  </li>
                </ul>
              </div>
              <div class="border-l-4 border-red-500 pl-4 mb-4">
                <h4 class="font-bold block mb-2">Available Games</h4>
                <ul>
                  <li>
                    Game A
                    <button
                      class="flex justify-end mb-2 bg-green-500 text-white px-2 py-1 rounded"
                    >
                      Join
                    </button>
                  </li>
                  <li>
                    Game B
                    <button
                      class="flex justify-end mb-2 bg-green-500 text-white px-2 py-1 rounded"
                    >
                      Join
                    </button>
                  </li>
                  <li>
                    Game C
                    <button
                      class="flex justify-end mb-2 bg-green-500 text-white px-2 py-1 rounded"
                    >
                      Join
                    </button>
                  </li>
                  <li>
                    Game D
                    <button
                      class="flex justify-end mb-2 bg-green-500 text-white px-2 py-1 rounded"
                    >
                      Join
                    </button>
                  </li>
                </ul>
              </div>
            </div>
          </div>

          <!-- Rules Container -->
          <div id="rulesContent" class="h-[700px] overflow-y-auto">
            <h2 class="text-lg font-bold mb-4 text-red-500">Rules Container</h2>
            <div class="rules-content">
              <p class="border-l-4 border-red-500 pl-4 mb-4">
                <span class="font-bold block mb-2">Setup:</span>
                The deck consists of 108 cards - 25 cards in each of red, green,
                blue, and yellow, with values of 0-9 in each color. There are
                also 8 "Draw Two" and "Reverse" cards, and 4 "Skip" and "Wild"
                cards. Choose a dealer and deal each player 7 cards. Place the
                remaining cards face down in the center to form a draw pile.
                Flip over the top card to start the discard pile.
              </p>

              <p class="border-l-4 border-red-500 pl-4 mb-4">
                <span class="font-bold block mb-2">Playing the Game:</span>
                Players take turns matching a card from their hand to the card
                on the top of the discard pile, either by number, color, or
                symbol (symbols are Draw Two, Reverse, and Skip). If you don't
                have a matching card, you must draw from the draw pile until you
                get a playable card.
              </p>

              <p class="border-l-4 border-red-500 pl-4 mb-4">
                <span class="font-bold block mb-2">Special Cards:</span>
                Skip: When played, skip the next player. Reverse: Reverses
                direction of play. Draw Two: Next player draws two cards and
                misses a turn. Wild: Play any color you want and state the new
                color. Wild Draw Four: Player gets to choose new color and next
                player draws four cards and misses a turn. Can only be played if
                the player doesn't have a card in the color led.
              </p>

              <p class="border-l-4 border-red-500 pl-4 mb-4">
                <span class="font-bold block mb-2"
                  >Going Out (Getting Uno):</span
                >
                When you get down to one card left, you must say "Uno" to alert
                other players. Failing to do so results in drawing two more
                cards if caught. You can go out by discarding your
                second-to-last card if it matches the upcard, then discarding
                your last card. The first player to get rid of all their cards
                wins! Players score points for cards left in opponents' hands.
              </p>
            </div>
          </div>
        </div>

        <!-- Lobby Container -->
        <div class="w-1/3 p-4 border border-pink-500">
          <h2 class="text-lg font-bold mb-4">Lobby Container</h2>
          <p>Welcome, <%= user.username %></p>
          <br />
          <div class="flex flex-col justify-center items-center">
            <form
              id="roomForm"
              class="flex flex-col justify-center items-center mt-4"
            >
              <label
                for="maxPlayers"
                class="block text-base font-bold text-gray-700 w-32 text-center"
                >Max Players</label
              >
              <br />
              <select
                id="maxPlayers"
                name="maxPlayers"
                class="mt-1 block w-32 py-2 px-3 border border-gray-300 bg-white rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 text-base"
              >
                <option value="2">2</option>
                <option value="3">3</option>
                <option value="4">4</option>
              </select>
              <br />
              <label
                for="roomName"
                class="block text-base font-bold text-gray-700 w-32 text-center"
                >Room Name</label
              >
              <input
                id="roomName"
                name="roomName"
                type="text"
                maxlength="20"
                class="mt-1 block w-32 py-2 px-3 border border-gray-300 bg-white rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 text-base"
                required
              />
              <button
                type="submit"
                class="bg-blue-500 text-white px-4 py-2 rounded mt-4"
              >
                Enter
              </button>
            </form>
            <!-- Room Card -->
            <!-- <div id="roomCard"
                  class="hidden flex flex-col justify-center items-center mt-4 border border-blue-500 p-4 rounded">
                  <h3 id="roomNameDisplay" class="text-lg font-bold mb-4 text-blue-500"></h3>
                  <p id="maxPlayersDisplay" class="mb-4"></p>
                  <div class="flex space-x-4">
                    <button class="bg-blue-500 text-white px-2 py-1 rounded mt-4">
                      Start Game
                    </button>
                    <button id="closeRoomCard" class="bg-red-500 text-white px-2 py-1 rounded mt-4">
                      Close
                    </button>
                  </div>
                </div>
                <br />
                <button class="bg-red-500 text-white px-4 py-2 rounded mt-4">
                  Join a Game
                </button> -->
            <form action="/logout" method="post">
              <button
                type="submit"
                class="bg-green-500 text-white px-4 py-2 rounded mt-4"
              >
                Logout
              </button>
            </form>
          </div>
        </div>

        <%- include("./layout/chat-box") %>
      </div>
    </div>

    <script>
      document
        .getElementById("toggleContent")
        .addEventListener("click", function () {
          let gamesContainer = document.getElementById("gamesContainer");
          let rulesContent = document.getElementById("rulesContent");
          gamesContainer.classList.toggle("hidden");
          rulesContent.classList.toggle("hidden");

          let button = document.getElementById("toggleContent");

          if (button.textContent === "Rules") {
            button.textContent = "Games";
          } else {
            button.textContent = "Rules";
          }
        });

      document
        .getElementById("roomForm")
        .addEventListener("submit", function (event) {
          event.preventDefault();
          let roomName = document.getElementById("roomName").value;
          let maxPlayers = document.getElementById("maxPlayers").value;
          document.getElementById("roomNameDisplay").textContent = roomName;
          document.getElementById("maxPlayersDisplay").textContent =
            "Max Players: " + maxPlayers;
          document.getElementById("roomCard").classList.remove("hidden");
        });

      document
        .getElementById("closeRoomCard")
        .addEventListener("click", function () {
          document.getElementById("roomCard").classList.add("hidden");
        });
    </script>
  </body>
</html>
